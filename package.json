{
  "name": "rtlify-for-claude",
  "version": "1.0.1",
  "description": "Open-source Chrome extension that displays right-to-left text in Claude AI.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "check": "node --check src/constants.js && node --check src/detect.js && node --check src/strings.js && node --check src/content.js && node --check src/popup.js && node --check src/background.js",
    "test": "node --test",
    "ci": "npm run check && npm test",
    "icons": "python3 scripts/make_icons.py",
    "build": "python3 scripts/build_zip.py"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MaherSaleem/rtlify-for-claude.git"
  },
  "license": "MIT"
}
